{
  "name": "ton.validate",
  "version": "1.0.1",
  "description": "A lightweight TypeScript enum library for JS/TS projects",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint 'src/**/*.{ts,js}' --fix",
    "prepare": "npm run build"
  },
  "keywords": [
    "enum",
    "typescript",
    "javascript",
    "library"
  ],
  "author": "kebin <mk9007@163.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^28.1.3",
    "ts-jest": "^29.4.1",
    "typescript": "^5.0.0",
    "eslint": "^8.0.0"
  },
  "engines": {
    "node": ">=16"
  }
}
